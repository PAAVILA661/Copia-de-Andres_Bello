// Archivo temporal para verificar las exportaciones de Prisma
import * as PrismaModule from '@prisma/client';

// Imprimir todas las exportaciones disponibles
console.log('Exportaciones disponibles en @prisma/client:');
console.log(Object.keys(PrismaModule));
